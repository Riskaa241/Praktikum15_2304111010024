Praktikum 15 -Telemetry ThingsBoard

Deskripsi:
Program ini digunakan untuk mengambil data telemetry dari device di ThingsBoard menggunakan REST API.

Metode:
- curl (GET request)
- Python (requests)

Endpoint:
GET /api/plugins/telemetry/DEVICE/{device-id}/values/timeseries
